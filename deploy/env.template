# 用户级环境变量（管理员开通时填写，FEAT-02 / §4.5 配置职责）
# 本文件由 provision-user.sh 复制到 deploy/users/<userId>/.env，不进 git。
#
# 注意：LLM API key 与 IM 渠道凭证（含 allowed_users 用户绑定）不在这里——
# 它们按 GA 官方约定写在同目录的 mykey.py（从 mykey_template.py 派生），
# 由 compose 只读挂载到 /opt/generic-agent/mykey.py。

# GA 前端：tui | wecom | dingtalk | tg（provision-user.sh --im 会写入）
GA_FRONTEND=tui

# --- 目标平台 API token（token 直连优先于浏览器登录态，FEAT-04）---
# 例：GITHUB_TOKEN=
